# Published Data Snapshots

This directory contains static snapshots of the pipeline data for GitHub Pages.

**DO NOT EDIT MANUALLY.**

To update these files:
1) Run the pipeline locally (e.g., `python make_contract.py` or `python run_pipeline.py`).
2) Run `python publish_data.py`.
3) Commit and push.
